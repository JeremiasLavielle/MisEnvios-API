spring.application.name=misenvios-api
server.port=8081
spring.profiles.active=dev

# JPA
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}
